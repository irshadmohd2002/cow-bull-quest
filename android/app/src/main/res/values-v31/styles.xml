<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ (API 31+) system splash screen. Uses the platform's own
     SplashScreen theme attributes directly - no androidx core-splashscreen
     dependency needed. The animated icon reuses the same adaptive-icon
     foreground emblem as the launcher icon and the pre-31 splash drawable,
     so all three stay visually identical. -->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>
